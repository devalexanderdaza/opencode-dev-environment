# Git Ignore Configuration
# Specifies intentionally untracked files that Git should ignore

# ===========================================
# CRITICAL: Override global gitignore
# ===========================================
# The global ~/.gitignore_global ignores /.opencode/ for symlinked repos.
# But THIS repo is the SOURCE of .opencode/ content - it MUST be tracked.
# The negation pattern below overrides the global ignore.
!.opencode/

.DS_Store

# Development
node_modules/

# Database files (contain personal memory/session data)
.opencode/skill/system-spec-kit/database/

# MCP server source code (install separately, not distributed)
.opencode/skill/mcp-narsil/mcp_server/
.opencode/skill/mcp-code-mode/mcp_server/

# Archive folder (root level only)
/z — Archive/
# Narsil index (project-local)
.narsil-index/

# Environment files
.env
.env.*
.env.local
.env.*.local

# Log files
*.log

# Temporary files
tmp/
scratch/
